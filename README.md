# DNS_BIND-helper
a dns helper for dns servers to bind better

If your unfamiliar with dns or bind. you can use the ip_helper app instead
its a fork and still in pre release

EDIT: this works on mac os.
mac os tutorial (terminal)

chmod +x bind_helper.py (cd to the directory where the .py is)
usage on mac ./bind_helper.py --example


